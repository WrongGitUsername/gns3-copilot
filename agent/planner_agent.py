# Responsible for breaking down initial goals into subtask lists (JSON format) and storing them in shared memory.
# Prompt focus: Emphasize that output format must be structured (e.g., JSON or YAML list) for easy parsing by subsequent agents.
