# 用于记录使用中的想法和问题

1、关于在模拟器中的拓扑环境制造故障的问题。
    - 需要明确告诉LLM, “你可以调用工具对拓扑中的设备进行任意操作制造一个故障。”
    ~~- 需要配置no ip domain-lookup命令。这样可以让llm多执行几个exit命令，退出到用户模式，会刷屏一下，我们不会直接看到llm制造故障的配置。~~ 关掉console窗口就行了。不要去点开工具的调用过程看。 就可以让LLM制造故障，自己练习了。
    
2、gns3_topology_reader工具执行后，返回的结果中，没有node的状态，比如是启动还是未启动。

3、需要找个合适的地方，提示词或工具描述中，告诉LLM， 不要执行重置协议的操作，或者不要执行需要输入yes/no的操作，如果有，请让告诉用户命令让用户自己来操作。

4、需要告诉LLM，这是一个网络模拟器，没有直接连接互联网，在操作linux系统的时候，不必尝试使用apt/yum等方式进行软件包的安装。

5、ui_model/settings.py中的一些配置输入，考虑增加一个验证。   嗯，可选，闲了再说把。

6、gns3_client/custom_gnsfy.py模块中，增加一个支持gns3 server v2/v3 的 链路抓包的函数。

