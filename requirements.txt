# GNS3 Copilot Dependencies
# Core AI and Automation Framework
langchain>=1.1.0
langchain-deepseek>=1.0.1
langchain-core>=1.0.5
langchain-google-genai>=1.0.0
langgraph>=1.0.0
langgraph-cli>=0.4.4
langgraph-cli[inmem]>=0.4.4

# Network Automation
netmiko>=4.6.0
nornir>=3.5.0
nornir-netmiko>=1.0.1
nornir-utils>=0.2.0
nornir-salt>=0.22.5

# Environment Management
python-dotenv>=1.2.1

# HTTP Requests (for GNS3 API)
requests>=2.32.5
urllib3>=2.5.0

# Data Validation
pydantic>=2.5.0

# Authentication
PyJWT>=2.10.1

# Display and Visualization
IPython>=8.0.0

# Type Extensions
typing-extensions>=4.0.0

# Telnet Client
telnetlib3>=2.0.8

# Web UI
streamlit>=1.32.0

# Additional dependencies found in codebase
# Note: gns3fy is not needed as we use custom gns3_client package
# Note: dotenv is already included via python-dotenv
# Note: functools, dataclasses, typing are built-in modules
# Note: math, time, pprint are built-in modules
# Note: log_config is a local package
# Note: public_model is a local package
# Note: importlib.metadata and pkg_resources are built-in/setuptools dependencies
